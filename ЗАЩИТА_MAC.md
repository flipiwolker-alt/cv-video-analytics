# 🎓 Шпаргалка защиты — запуск на Маке (Apple Silicon)

Всё считается **локально на GPU Мака** (Metal/MPS). Интернет нужен один раз —
скачать модели. На самой защите интернет НЕ обязателен (грузишь видеофайл с диска).

---

## ⏱ СЕГОДНЯ ВЕЧЕРОМ (один раз, ~20–30 мин — модели качаются заранее!)

Открой **Терминал** на Маке и выполни:

```bash
# Если папка уже есть — обновим, иначе склонируем
[ -d ~/cv-video-analytics ] && git -C ~/cv-video-analytics pull || git clone https://github.com/flipiwolker-alt/cv-video-analytics.git ~/cv-video-analytics
cd ~/cv-video-analytics
bash scripts/setup_mac.sh
```

Скрипт поставит ffmpeg + Ollama + модель `qwen2.5:7b` + Python-зависимости
(torch с MPS встанет сам) и запустит интерфейс.

Когда откроется интерфейс — **прогони один короткий ролик (30–60 сек)** через
загрузку файла. Это докачает CV-модели (~3–4 ГБ) и закэширует их. Готово.

> Если `setup_mac.sh` где-то споткнётся — пришли мне текст ошибки.

---

## 🎬 НА ЗАЩИТЕ (запуск за 30 секунд)

```bash
cd ~/cv-video-analytics
source .venv/bin/activate
ollama serve >/dev/null 2>&1 &      # поднять LLM, если не запущен
python run_ui.py
```

→ открой в браузере **http://localhost:7860**
→ **перетащи видеофайл** в поле «📁 Загрузить видеофайл» (работает офлайн!)
→ выбери пресет → «Запустить анализ».

(Поле YouTube-ссылки — альтернатива, если есть интернет.)

---

## ✅ Советы для надёжной демонстрации

1. **Короткий клип (30–60 сек)** — анализ быстрый, не ждёшь перед комиссией.
2. Убедись, что `ollama serve` запущен — LLM-слой это главная «фишка» точности.
3. Хочешь показать по ссылке (проектор/телефон) — `python run_ui.py --share`
   (нужен интернет) → публичный URL `*.gradio.live`.

---

## 📊 Что показывать (суть проекта)

**12 категорий нарушений** по **8 каналам анализа:**

| Канал | Технология | Что ловит |
|---|---|---|
| Речь | Whisper + словарь | мат, угрозы, пропаганда |
| Текст в кадре | EasyOCR | надписи, баннеры, субтитры |
| Объекты | YOLOv11 + YOLO-World | алкоголь, сигареты, оружие… |
| Сцена | SigLIP 2 | общий контекст кадра |
| Порнография | Falconsai NSFW | нагота/порно |
| Действия | X-CLIP (видео-окно) | драки, вандализм (динамика) |
| Контекст | **LLM qwen2.5** | смысл речи, а не только слова |

**Категории:** алкоголь, наркотики, курение, экстремизм, ЛГБТ-пропаганда, насилие,
вандализм, азартные игры, мат, порнография, суицид/селфхарм, жестокость к животным.

**Главная «фишка»:** LLM понимает *смысл* (напр. «поставь всё на красное в казино»
→ лудомания), а деобфускация ловит замаскированный мат (`b!tch`, `з*ебись`).
